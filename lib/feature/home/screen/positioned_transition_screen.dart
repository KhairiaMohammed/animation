import 'package:flutter/material.dart';
import '../../positioned_transition/widget/widget.dart';

class PositionedTransitionScreen extends StatelessWidget {
  const PositionedTransitionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return PositionedTransitionWidget();
  }
}
