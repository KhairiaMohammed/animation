import 'package:flutter/material.dart';
import '../../Size_transition/widget/widget.dart';

class SizeTransitionScreen extends StatelessWidget {
  const SizeTransitionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizeTransitionWidget();
  }
}
